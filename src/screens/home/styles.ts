import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    constainer: {
        alignItems: "center",
        paddingTop: 100
    },
    logo: {
        top: 20,
        height: 150,
        maxWidth: "60%",
        resizeMode: "contain"
    },
    buttons: {
        marginTop: 80
    },
    button: {
        marginBottom: 20
    }
});

export default styles;
